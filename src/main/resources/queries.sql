create table HEALTHCARE_REGISTERED_USER
(
userid varchar(2000) NOT NULL,
firstname  varchar(2000) NOT NULL,
lastname varchar(2000) NOT NULL,
email varchar(2000) NOT NULL,
encodedpassword varchar(2000) NOT NULL,
contact varchar(2000) NULL
primary key(userid)
)